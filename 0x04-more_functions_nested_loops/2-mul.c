#include "main.h"
#include <stdio.h>

/**
 * mul - this fuction will Multiply 2 integers.
 * @a : First integer
 * @b : Second integer
 * Return: Return the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
